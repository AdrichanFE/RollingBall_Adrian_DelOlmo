using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Vectores : MonoBehaviour
{



    // Start is called before the first frame update
    void Start()
    {
        //transform.position = new Vector3(0, 0, 0); Para la posicion del objeto
        //transform.eulerAngles = new Vector3(0, 0, 0); Para la rotacion del objeto
        //transform.localScale=new Vector3(1, 1, 1); Para la escala del propio objeto
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
